# Follow-up TODOs - 2026-04-01

## 背景

基于 `2026-04-01` 当前代码与文档状态，下面这些事项仍需 follow-up。

当前已完成：

- 数据契约对齐
- 高优先级退款/停用账户问题修复
- 消费路径 phase 1 优化：`LIMIT + FOR UPDATE`
- 数据库与架构文档补齐

## P1

### 1. 费用拆账真正落地

目标：

- 在消费主流程中真正写入 `transaction_line_items`
- 区分基础 credits 成本和附加费用成本
- 让 `includeFees` 在退款流程里具备真实业务含义

现状：

- `transaction_line_items` 表已存在
- `rate_card_items.fee_category_id` 已建模
- `RefundRequest.includeFees` 已建模
- 主流程仍未做费用级拆账和费用级退款

建议动作：

- 消费时按基础费用和附加费用生成 line items
- 退款时支持按 line item 精确冲回
- 增加费用级别的收入统计和退款测试

### 2. grant 过期状态维护

目标：

- 让 `grant_status` 不只依赖查询时判断，而是能被持续维护为真实状态

现状：

- 当前主要依赖 `sort_expires_at > CURRENT_TIMESTAMP` 过滤
- 未见专门的定时任务或批量任务把过期 grant 刷成 `expired`

建议动作：

- 增加定时任务或批处理，批量更新过期 grant
- 明确过期 grant 对余额快照和报表的影响边界
- 补充过期状态相关自动化测试

### 3. 消费并发优化第二阶段

目标：

- 进一步降低同一账户高并发消费时的锁等待

现状：

- 当前已完成 phase 1：小批量 `LIMIT + FOR UPDATE`
- 尚未引入 `SKIP LOCKED`
- `credit_balances` 仍然是账户级热点更新行

建议动作：

- 评估 MySQL 8.0 下 `FOR UPDATE SKIP LOCKED`
- 做并发消费压测或至少补并发集成测试
- 评估 `credit_balances` 是否需要进一步演进

## P2

### 4. 消费循环中的 grantType 查询优化

目标：

- 消除消费循环里按 grant 逐条查 `grantType` 的小型 N+1

现状：

- 当前每扣减一个 grant，都会额外查一次 `grant_types`
- grant 数量较多时会产生额外 SQL 开销

建议动作：

- 预取本批次需要的 `grantType`
- 或把余额桶判断所需语义继续下沉到 `credit_grants`

### 5. 测试补强

目标：

- 把更接近生产风险的边界覆盖到自动化测试里

建议优先补充：

- 并发消费测试
- 幂等消费重复提交测试
- grant 过期刷新测试
- 费用级退款测试
- line item 写入测试

### 6. SPA fallback 的 API 边界修正

目标：

- 避免未知 API 请求被前端 `index.html` 吞掉

现状：

- `WebConfig` 当前对未知路径直接 fallback 到 `index.html`
- 注释写的是“非 API 路径 fallback”，但代码未显式排除 `/api/**`

建议动作：

- 在 fallback 中排除 `/api/**`
- 同时排除 `/swagger-ui/**`、`/v3/api-docs/**` 和静态资源请求
- 增加对应的 MVC 或集成测试

## 推荐推进顺序

1. 费用拆账真正落地
2. grant 过期状态维护
3. 消费并发优化第二阶段
4. 消费循环中的 grantType 查询优化
5. 测试补强
6. SPA fallback 的 API 边界修正

## 参考文档

- `docs/ARCHITECTURE.md`
- `docs/DATABASE.md`
- `docs/CONSUMPTION_PATH_OPTIMIZATION.md`
